rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Regra geral: permite leitura e escrita em qualquer documento
    // apenas para usuários autenticados.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
