rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Permite acesso de leitura e escrita a qualquer arquivo
    // somente se o usuário estiver autenticado.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
